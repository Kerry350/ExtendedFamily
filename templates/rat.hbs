{{> header}}

<div class="page-header">
	<h1>{{name}}</h1>
</div>

{{{contents}}}

<div class="page-header">
	<h2>Gallery</h2>
</div>

<div class="panel panel-default">
  <div class="panel-body">
    {{#generateGallery name images}}
      <img src="{{this}}" />
    {{/generateGallery}}
  </div>
</div>

{{> footer}}

<ul>
  {{#each collections.rabbits}}
    <li>
      <h3>{{this.name}}</h3>
      <a href="{{this.href}}">{{this.name}}</a>
    </li>
  {{/each}}

  <ul>
    {{#each images.rabbits}}
      <li>
        <img src="{{this}}" />
      </li>
    {{/each}}
  </ul>
</ul>